<div class="row">
  <div class="col-md-12">
    <div class="pull-right">
      <button class="btn btn-default">Report Map Comment/Correction</button>
      <% if signed_in? %>
      <%= link_to "Logout", session_path, method: :delete, class: "btn btn-default" %>
      <% else %>
      <%= link_to "Login/Create Account", new_session_path, class: "btn btn-default" %>
      <% end %>
    </div>
  </div>
</div>
